# Contributing

1. Fork and clone the repo.
2. Make changes to lib/client.js or lib/index.js.
3. Run `node --check` on both files.
4. Open a pull request.

Install locally: `dsh plugin --profile web add dsh-message-outline`
